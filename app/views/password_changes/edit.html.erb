<section>
  <h1>Change Password</h1>
  <p>You must update your password before continuing.</p>

  <%= form_with scope: :password_change, url: password_change_path, method: :patch do |form| %>
    <div>
      <%= form.label :current_password %>
      <%= form.password_field :current_password, required: true, autocomplete: "current-password" %>
    </div>

    <div>
      <%= form.label :password, "New password" %>
      <%= form.password_field :password, required: true, minlength: 12, autocomplete: "new-password" %>
    </div>

    <div>
      <%= form.label :password_confirmation, "Confirm new password" %>
      <%= form.password_field :password_confirmation, required: true, minlength: 12, autocomplete: "new-password" %>
    </div>

    <%= form.submit "Update password", class: "primary-button" %>
  <% end %>
</section>
