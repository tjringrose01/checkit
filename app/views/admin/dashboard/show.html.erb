<section class="page-header">
  <div>
    <p class="eyebrow">Admin Workspace</p>
    <h1>Manage Checkit</h1>
    <p>Control checklist publishing, checklist items, CSV imports, and account unlocks.</p>
  </div>

  <div class="admin-actions">
    <%= link_to "Back To User View", root_path, class: "secondary-link" %>
    <%= link_to "New Checklist", new_admin_checklist_path, class: "primary-link" %>
  </div>
</section>

<section class="checklists">
  <article class="checklist-card">
    <header class="checklist-card__header">
      <div>
        <p class="eyebrow">Locked Accounts</p>
        <h2>User Access</h2>
      </div>
    </header>

    <% if @locked_users.any? %>
      <ul class="checklist-items">
        <% @locked_users.each do |user| %>
          <li class="checklist-item">
            <div class="checklist-item__content">
              <h3><%= user.user_id %></h3>
              <p><%= user.email %></p>
              <p>Failed attempts: <%= user.failed_login_attempts %></p>
            </div>

            <%= button_to "Unlock User", unlock_admin_user_path(user), method: :patch, class: "primary-button" %>
          </li>
        <% end %>
      </ul>
    <% else %>
      <p>No locked users.</p>
    <% end %>
  </article>

  <article class="checklist-card">
    <header class="checklist-card__header">
      <div>
        <p class="eyebrow">Checklist Catalog</p>
        <h2>Current Checklists</h2>
      </div>

      <%= link_to "Open Full Checklist Manager", admin_checklists_path, class: "secondary-link" %>
    </header>

    <% if @checklists.any? %>
      <ul class="checklist-items">
        <% @checklists.each do |checklist| %>
          <li class="checklist-item">
            <div class="checklist-item__content">
              <h3><%= checklist.title %></h3>
              <p>Status: <%= checklist.status.titleize %></p>
              <p><%= pluralize(checklist.checklist_items.count, "item") %></p>
            </div>

            <%= link_to "Edit Checklist", edit_admin_checklist_path(checklist), class: "secondary-link" %>
          </li>
        <% end %>
      </ul>
    <% else %>
      <p>No checklists yet.</p>
    <% end %>
  </article>
</section>
